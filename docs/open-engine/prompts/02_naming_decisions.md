Help me make the naming decisions for my Open Engine setup.

Ask one focused question at a time. Do not create anything yet. By the end, give me a concise setup plan I can use in Linear.

I need decisions for:
1. The Linear team that will own agent work.
2. The Linear project name: Personal Agent Engine or Team Agent Engine, unless there is a better name for my situation.
3. The exact label name. Default: agent-instructions.
4. My agent codes. Use stable lowercase codes like alex-codex, alex-claude, or sam-codex.
5. The private setup issue title. Default: [agent instructions][all agents][standing_skill] Install Open Agent Engine core context v1.
6. The status ledger issue title. Default: [agent instructions][all agents][standing_status] Open Agent Engine status ledger.
7. The optional standing skill directory issue title. Default: [agent instructions][all agents][optional_standing_skill_directory] Open Engine optional skill directory.
8. The local private context path for each runtime.

Rules:
- Keep the first version simple enough for one person to maintain.
- Use the same names consistently in every prompt, issue, automation, and receipt.
- If I have multiple runtimes, map each runtime to one agent code.
- If I have a team, route work to the human who owns the target agent.