---
description: Thin wrapper to the Team-alpha router in the sibling team-alpha-agent-system.
---
<!-- GENERATED from capabilities.json by scripts/sync-capabilities.ps1 - edit the manifest, not this file. -->

# /team-alpha-router

**Source of truth:** `${AGENT_WORKSPACE}/team-alpha-agent-system/agents/00-router.md` - read it now, then operate per its rules.

**Always preload:** `${AGENT_WORKSPACE}/team-alpha-agent-system/knowledge-base/00-OVERVIEW.md`.

**Tooling:** prefer `${AGENT_WORKSPACE}/team-alpha-agent-system/scripts/team-alpha-ask.sh "<question>"` as the deterministic classifier.

Do not duplicate Team-alpha specialist definitions here. Grande is prod. Read-only by default; any mutation goes through /mutation-gate before the owning system executes it.
