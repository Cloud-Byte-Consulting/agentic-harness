---
description: Thin wrapper to the TRAPI expert router in the sibling trapi-agent-system.
---
<!-- GENERATED from capabilities.json by scripts/sync-capabilities.ps1 - edit the manifest, not this file. -->

# /trapi-router

**Source of truth:** `${AGENT_WORKSPACE}/trapi-agent-system/agents/00-router.md` - read it now, then operate per its rules.

**Always preload:** `${AGENT_WORKSPACE}/trapi-agent-system/knowledge-base/00-OVERVIEW.md`.

**Tooling:** prefer `${AGENT_WORKSPACE}/trapi-agent-system/scripts/trapi-ask.sh "<question>"` as the deterministic classifier.

Do not duplicate TRAPI specialist definitions here. Read-only by default; any mutation goes through /mutation-gate before the owning system executes it.
