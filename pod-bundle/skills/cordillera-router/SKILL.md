---
description: Thin wrapper to the Cordillera router in the sibling cordillera-agent-system.
---
<!-- GENERATED from capabilities.json by scripts/sync-capabilities.ps1 - edit the manifest, not this file. -->

# /cordillera-router

**Source of truth:** `${AGENT_WORKSPACE}/cordillera-agent-system/agents/00-router.md` - read it now, then operate per its rules.

**Always preload:** `${AGENT_WORKSPACE}/cordillera-agent-system/knowledge-base/00-OVERVIEW.md`.

**Tooling:** prefer `${AGENT_WORKSPACE}/cordillera-agent-system/scripts/cordillera-ask.sh "<question>"` as the deterministic classifier.

Do not duplicate Cordillera specialist definitions here. Grande is prod. Read-only by default; any mutation goes through /mutation-gate before the owning system executes it.
